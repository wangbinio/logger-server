# 任务：v0.1 发布收尾

## 背景

项目主体功能已完成，需要补齐完整架构文档、更新 README，并将当前工程发布为 `v0.1`。

## 计划

- [x] 基于当前源码梳理运行架构、模块职责、消息流、数据落库模型和配置模型。
- [x] 新增 `ARCHITECTURE.md`，覆盖核心架构、运行链路、可靠性边界、测试策略和 v0.1 范围。
- [x] 更新 `README.md`，移除过期的阶段描述和 `local` 配置说明，补充启动、配置、测试、发布信息。
- [x] 将 Maven 版本收敛为 v0.1 对应的发布版本。
- [x] 执行测试与打包验证。
- [x] 提交发布改动并创建 `v0.1` 标签。

## Review

- 已新增 `ARCHITECTURE.md`：
  - 覆盖系统定位、总体架构、包结构、RocketMQ topic、平台协议、生命周期链路、TDengine 数据模型、配置模型、异常指标、并发资源管理、测试策略和 v0.1 范围。
- 已更新 `README.md`：
  - 将阶段性开发说明改为发布说明。
  - 移除过期的 `application-local.yml` 源码配置描述。
  - 补充当前两份 YAML、构建测试命令、运行命令、消息主题和 v0.1 发布范围。
- 已将 Maven 版本从 `1.0.0-SNAPSHOT` 调整为 `0.1.0`，构建产物为 `target/logger-server-0.1.0.jar`。
- 已完成验证：
  - `mvn -q test` 通过。
  - `mvn -q package` 通过。
- 发布动作：
  - 提交发布改动。
  - 创建 `v0.1` 标签。
